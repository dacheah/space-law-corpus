{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://space-law-corpus/schema/authoritative-metadata.schema.json",
  "title": "Authoritative document metadata",
  "description": "Provenance record for a single authoritative document version. See docs/design/01-provenance-schema.md. Every required field must be populated before a document enters the authoritative layer. Verification refinement (2026-07-02): optional 'verification' (object) and 'corrections' (array) fields record fidelity verification and documented text corrections; the text and its hashes remain the source of truth and Git preserves every prior state.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "corpus_id",
    "version_id",
    "title",
    "jurisdiction",
    "document_type",
    "official_citation",
    "authentic_languages",
    "source_url",
    "source_publisher",
    "source_is_official",
    "retrieval_date",
    "retrieved_by",
    "original_format",
    "original_filename",
    "content_hash",
    "original_sha256",
    "language",
    "authoritative_status",
    "license",
    "capture_history",
    "provenance_note",
    "binding_force",
    "issuing_authority"
  ],
  "properties": {
    "corpus_id": {
      "type": "string",
      "description": "Stable internal identifier, e.g. un/treaty/ost-1967"
    },
    "version_id": {
      "type": "string",
      "description": "Version identifier, usually a date, e.g. 1967-01-27"
    },
    "title": {
      "type": "string",
      "description": "Official title, verbatim"
    },
    "short_title": {
      "type": "string"
    },
    "jurisdiction": {
      "type": "string",
      "description": "international/UN or ISO 3166-1 alpha-3"
    },
    "document_type": {
      "type": "string",
      "enum": [
        "treaty",
        "ga_resolution",
        "national_legislation",
        "soft_law_guideline",
        "arbitration_rules"
      ]
    },
    "binding_force": {
      "type": "string",
      "enum": [
        "binding",
        "non_binding"
      ],
      "description": "Legal force of the instrument on its own accord: binding (imposes legal obligations enforceable in some legal order) or non_binding (recommendatory soft law, no obligation until adopted by a binding instrument). Orthogonal to authoritative_status. Instrument tier is DERIVED from jurisdiction downstream."
    },
    "issuing_authority": {
      "type": "string",
      "description": "Body that made/adopted the instrument (e.g. United Nations General Assembly, United States Congress). Distinct from source_publisher (where the text was obtained)."
    },
    "administering_authority": {
      "type": [
        "string",
        "null"
      ],
      "description": "Body that administers/enforces the instrument where different from the issuer; null if not applicable."
    },
    "official_citation": {
      "type": "string"
    },
    "authentic_languages": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "ISO 639 codes of all languages in which the instrument is authentic"
    },
    "adoption_date": {
      "type": [
        "string",
        "null"
      ],
      "description": "ISO 8601 date or null (with reason in provenance_note)"
    },
    "entry_into_force_date": {
      "type": [
        "string",
        "null"
      ]
    },
    "effective_date": {
      "type": [
        "string",
        "null"
      ]
    },
    "source_url": {
      "type": "string",
      "format": "uri"
    },
    "source_publisher": {
      "type": "string"
    },
    "source_is_official": {
      "type": "boolean"
    },
    "retrieval_date": {
      "type": "string",
      "format": "date"
    },
    "retrieved_by": {
      "type": "string"
    },
    "original_format": {
      "type": "string",
      "enum": [
        "pdf",
        "html",
        "docx",
        "txt",
        "other"
      ]
    },
    "original_filename": {
      "type": "string"
    },
    "content_hash": {
      "type": "string",
      "pattern": "^sha256:"
    },
    "original_sha256": {
      "type": "string",
      "pattern": "^sha256:"
    },
    "text_sha256": {
      "type": "string",
      "pattern": "^sha256:",
      "description": "Required when a text.txt exists"
    },
    "text_fidelity": {
      "type": "string",
      "enum": [
        "verbatim_transcription",
        "extracted_verified",
        "extracted_unverified",
        "ocr_unverified",
        "not_applicable"
      ],
      "description": "Required when a text.txt exists"
    },
    "language": {
      "type": "string",
      "description": "ISO 639 code of the stored text.txt"
    },
    "authoritative_status": {
      "type": "string",
      "enum": [
        "authentic_text",
        "official_consolidation",
        "certified_copy",
        "authoritative_missing"
      ]
    },
    "license": {
      "type": "string",
      "description": "Terms governing the SOURCE text, e.g. UN-materials-terms, public-domain-gov"
    },
    "rights_note": {
      "type": "string"
    },
    "capture_history": {
      "type": "array",
      "minItems": 1,
      "description": "Append-only list of retrieval events",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "date",
          "source_url",
          "original_sha256"
        ],
        "properties": {
          "date": {
            "type": "string",
            "format": "date"
          },
          "source_url": {
            "type": "string",
            "format": "uri"
          },
          "original_sha256": {
            "type": "string",
            "pattern": "^sha256:"
          },
          "note": {
            "type": "string"
          }
        }
      }
    },
    "supersedes": {
      "type": [
        "string",
        "null"
      ]
    },
    "superseded_by": {
      "type": [
        "string",
        "null"
      ]
    },
    "related_documents": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "provenance_note": {
      "type": "string",
      "minLength": 1
    },
    "verification": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string"
        },
        "method": {
          "type": "string"
        },
        "reference": {
          "type": "string"
        },
        "verified_by": {
          "type": "string"
        },
        "verified_date": {
          "type": "string",
          "format": "date"
        },
        "note": {
          "type": "string"
        }
      }
    },
    "corrections": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "date",
          "change"
        ],
        "properties": {
          "date": {
            "type": "string",
            "format": "date"
          },
          "change": {
            "type": "string"
          },
          "reference": {
            "type": "string"
          },
          "note": {
            "type": "string"
          }
        }
      }
    },
    "session": {
      "type": [
        "string",
        "null"
      ],
      "description": "Issuing body's session, e.g. 'Sixteenth session (XVI)'."
    },
    "adoption_mode": {
      "type": [
        "string",
        "null"
      ],
      "description": "How the instrument was adopted (e.g. 'adopted unanimously, 1085th plenary meeting'). Material to customary-law significance."
    },
    "agenda_item": {
      "type": [
        "string",
        "null"
      ],
      "description": "Agenda item under which it was adopted."
    },
    "corroborating_sources": {
      "type": "array",
      "description": "Independent record copies pinned by hash WITHOUT being stored (e.g. an ODS scan whose OCR is unusable as text).",
      "items": {
        "type": "object",
        "properties": {
          "symbol": {
            "type": [
              "string",
              "null"
            ]
          },
          "url": {
            "type": "string"
          },
          "sha256": {
            "type": [
              "string",
              "null"
            ]
          },
          "retrieved": {
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "type": [
              "string",
              "null"
            ]
          },
          "note": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "url"
        ],
        "additionalProperties": false
      }
    },
    "akn_uri": {
      "type": [
        "string",
        "null"
      ],
      "description": "Akoma Ntoso Naming Convention Work-level identifier, or null where no conformant one exists. Shared by every language version of the instrument — language belongs to the Expression, not the Work. Minted deterministically by scripts/akn.py from schema/akn-registry.json; never hand-written.",
      "pattern": "^/akn/[a-z]{2}(-[a-z0-9]{1,3})?/[A-Za-z][A-Za-z0-9]*(/[A-Za-z0-9._\\-]+)*/\\d{4}(-\\d{2}-\\d{2})?/[A-Za-z0-9._\\-]+$"
    },
    "akn_expression_uri": {
      "type": [
        "string",
        "null"
      ],
      "description": "AKN Expression identifier: the Work URI plus the ISO 639-2 language. Null where the Work could not be minted, or where the record's language cannot be expressed in ISO 639-2 (which has no script or region subtag)."
    },
    "akn_uri_basis": {
      "type": "string",
      "enum": [
        "akn4un",
        "akn4eu",
        "iso3166",
        "none",
        "unmapped"
      ],
      "description": "Why the identifier looks the way it does — or why there isn't one. 'none' means no conformant country code exists for the issuing body (the ISA, ITLOS, the ATS Secretariat, CCAMLR): permanent, and not something to paper over with an ISO user-assigned code. 'unmapped' means the registry does not cover this yet: our backlog, and it should trend to zero. Collapsing the two would hide the actionable one."
    },
    "akn_uri_note": {
      "type": "string",
      "description": "Required whenever basis is 'none' or 'unmapped': states the reason. An unexplained absence is indistinguishable from an oversight."
    }
  },
  "allOf": [
    {
      "$comment": "If authoritative_status is authoritative_missing there is no stored text, so text_sha256/text_fidelity are not required; otherwise they are.",
      "if": {
        "properties": {
          "authoritative_status": {
            "const": "authoritative_missing"
          }
        }
      },
      "then": {},
      "else": {
        "required": [
          "text_sha256",
          "text_fidelity",
          "language"
        ]
      }
    },
    {
      "$comment": "basis 'none'/'unmapped' requires a null akn_uri AND a non-empty note. Enforced here so a record cannot claim a decline while carrying an identifier, even if it bypasses akn.py.",
      "if": {
        "properties": {
          "akn_uri_basis": {
            "enum": [
              "none",
              "unmapped"
            ]
          }
        },
        "required": [
          "akn_uri_basis"
        ]
      },
      "then": {
        "properties": {
          "akn_uri": {
            "type": "null"
          },
          "akn_expression_uri": {
            "type": "null"
          },
          "akn_uri_note": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "akn_uri_note"
        ]
      }
    }
  ]
}
